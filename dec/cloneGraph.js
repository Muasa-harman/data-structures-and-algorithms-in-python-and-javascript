// Given a reference of a node in a connected undirected graph.
//  Return a deep copy (clone) of the graph .
// Each node in the graph contains a value (int) and a list(List | Node) of its neighbors
class Node {
  // public int val;
  // public List<Node> neighbors;
}

//  Test case format
// For simplicity each node's value is the same as the node's index(1-indexed).Eg,the first node
// with val --1,the second node with val --2 and so on. The graph is represented in the test case using an adjacency list.

// Input: adjList =  [[2,4],[1,3],[2,4],[1,3]]
// Output:[[2,4],[1,3],[2,4],[1,3]]

// Time:0(V+E)
// Space:0(V)
